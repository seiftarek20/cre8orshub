// Phase 1 placeholder.
// Real Supabase reward points and badge queries will be connected after the
// reward_points, badges, and student_badges tables are created.
export async function getStudentRewards() {
  return {
    points: [],
    badges: [],
  };
}
