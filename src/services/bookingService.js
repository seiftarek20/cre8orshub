// Phase 1 placeholder.
// Real Supabase booking request inserts will be connected after the
// booking_requests table and public insert policy are implemented.
export async function createBookingRequest() {
  return {
    data: null,
    error: null,
  };
}
