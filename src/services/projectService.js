// Phase 1 placeholder.
// Real Supabase project reads, writes, and future upload references will be
// connected after the projects table and storage policies are implemented.
export async function getStudentProjects() {
  return [];
}
