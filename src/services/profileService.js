// Phase 1 placeholder.
// Real Supabase profile reads and updates will be connected after the profiles
// table, RLS policies, and authenticated user flow are implemented.
export async function getCurrentUserProfile() {
  return null;
}
